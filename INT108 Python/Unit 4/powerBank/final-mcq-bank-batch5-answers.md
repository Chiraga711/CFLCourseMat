# Strings — Final MCQ Bank, Batch 5 Answer Key (Q201–Q235)

> For the instructor. Every fill-in answer verified by substituting it into the blank and executing.

**Q201 — A.** Index −1 is the last character, `o`.
**Q202 — B.** `[:3]` takes the first three characters → `win`.
**Q203 — C.** Step −1 reverses → `nohtyp`.
**Q204 — D.** `upper()` gives `SHOUT`.
**Q205 — A.** `strip()` removes both ends' whitespace.
**Q206 — B.** `replace('-', '+')` swaps every dash.
**Q207 — C.** `count('a')` in banana → 3.
**Q208 — D.** `split()` gives 2 words.
**Q209 — A.** Joining with `-` produces `a-b-c`.
**Q210 — B.** `in` tests membership among vowels → 2 (e, o).
**Q211 — C.** `find('p')` returns the first position, 1.
**Q212 — D.** `ch + r` prepends, reversing → `cba`.
**Q213 — A.** `:.2f` inside the braces → `3.14`.
**Q214 — B.** `endswith('.pdf')` → True.
**Q215 — C.** `ord('b')` is 98.
**Q216 — D.** `chr(65)` is `A`.
**Q217 — A.** A step of 2 over `[1:6]` picks positions 1, 3, 5 → `bdf`.
**Q218 — B.** Index −1 of the word list is the last word.
**Q219 — C.** `capitalize()` uppercases only the first letter and lowercases the rest.
**Q220 — D.** `int(ch)` converts each digit: 1+3+5 = 9.
**Q221 — A.** `s[-1]` is the standard last-character idiom.
**Q222 — B.** Only slicing reverses a string; strings have no `.reverse()`.
**Q223 — C.** `split(',')` divides on commas into a list.
**Q224 — D.** The separator string calls `join` on the list.
**Q225 — A.** `isdigit()` is the digits test; there is no `isnumber()`.
**Q226 — B.** Strings are immutable — you must build a new one: `'H' + s[1:]`.
**Q227 — C.** `count` is a string method called on `s`.
**Q228 — D.** `for ch in s` iterates characters directly.
**Q229 — A.** Methods return new strings; you must assign the result back.
**Q230 — B.** Comparing with the reversed slice is the palindrome test (`=` would assign, not compare).
**Q231 — C.** `find` returns −1 on a miss; `index` raises an error.
**Q232 — D.** Only `:.2f` guarantees two decimals; `round` drops trailing zeros.
**Q233 — A.** `strip()` is the both-ends trim; Python has no `trim()`.
**Q234 — B.** Split into words first, then take index 0.
**Q235 — C.** `replace` returns a new string — assign it back to keep it.

---

*Letter spread: A×9 · B×9 · C×9 · D×8. Difficulty: 3 [E] · 26 [M] · 6 [H].*